{
  "name": "cli",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rescript build && vite build",
    "clean": "rescript clean",
    "start": "node dist/cli.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@rescript/runtime": "^12.3.1",
    "core": "workspace:*"
  },
  "devDependencies": {
    "rescript": "^12.3.1",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}