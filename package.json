{
  "name": "sleight",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "test": "pnpm -r --if-present run test",
    "start": "pnpm --filter hello-cli run start"
  }
}
